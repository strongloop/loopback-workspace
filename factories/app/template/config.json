{
  {?port}
  "port": {port},
  {:else}
  "port": 3000,
  {/port}
  {?hostname}
  "hostname": {hostname},
  {:else}
  "hostname": "0.0.0.0",
  {/hostname}
  "middleware": [
    "compress",
    "favicon",
    "logger",
    "bodyParser",
    "methodOverride",
    "rest"
  ]
}

module.exports = function(app, options) {
  // Add your component bootstrap logic here
};

if (require.main === module) {
  console.log('The component should be run as part of a loopback application.');
}
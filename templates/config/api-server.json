{
  "name": "api-server",  
  "description": "A LoopBack API server with local User auth",
  "extends": "empty-server",
  "files": {
    "path": "/api-server"
  },
  "package": {
    "dependencies": {
      "loopback-connector-mysql": "^3.0.0"
    }
  }
}

/**
 * The server component template.
 */

var template = module.exports;
var component = template.component = {
  host: 'localhost'
};

template.package = {
  "name": "server",
  "main": "server.js"
};
